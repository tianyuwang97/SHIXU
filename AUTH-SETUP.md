# 时序：管理员预览登录

当前只启用一个管理员测试账号，暂无用户注册、邮件验证码或会员收费。登录页自动使用账号 admin，只需输入对应密码。

## 运行时配置

- ADMIN_USERNAME：admin。
- ADMIN_PASSWORD_VERIFIER：带随机盐的 PBKDF2-SHA256 校验值，100000 次迭代，存放于 Sites 的 secret，不存明文密码。
- 账号密码在网站源码和发布包之外单独交付给站点主人。不得把密码或校验值写入前端。
- 原先提供的 Supabase 项目配置保留供未来使用，当前登录不依赖它，邮件开关保持关闭。

## 权限范围

首页 A股、美股、港股的 R01、R02 完整筛选条件及个股入选解释只在管理员会话通过服务器校验后返回。未登录仍可查看候选股票、行情和规则编号。
对应的同行筛选计算放在服务器端，浏览器只接收匹配结果。基金自定义筛选和历史演练的教学规则不在本次权限范围内。

会话使用随机令牌，通过 HttpOnly、Secure、SameSite=Lax Cookie 保存，最长8小时。D1 仅保存令牌摘要。退出时删除服务器会话；更新密码校验值后旧会话自动失效。登录接口检查来源、限制请求体，并使用 D1 对尝试次数限流。

## 验证

运行 auth-access.test.mjs，使用真实 SQLite 和 Web Crypto 检查错误密码、伪造/过期会话、退出撤销、密码轮换、来源校验、限流及三个市场的数据脱敏。测试账号数据与正式密码完全分离。

未来有真实用户时，再接回托管的邮箱身份服务和独立用户权益；不要把这个共享预览账号当成公开注册系统。
